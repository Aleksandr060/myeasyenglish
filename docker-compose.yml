version: "3.9"

services:
  app:
    build: .
    ports:
      - "4000:4000"
    environment:
      PORT: 4000
      JWT_SECRET: change-me-in-production
      CLIENT_URL: http://localhost:4000
      DATABASE_URL: file:/data/prod.db
    volumes:
      - app_data:/data

volumes:
  app_data:
